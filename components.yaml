node:
  component-type: docker-image
  current-version: 10.2-alpine
  docker-repo: library
  filter: /.*alpine$/
  files: [Dockerfile]
behave:
  component-type: pypi
  current-version: 1.2.5
  files: [requirements.txt]
  filter: /^\d+\.\d+\.\d+$/
selenium:
  component-type: pypi
  current-version: 3.11
  files: [requirements.txt]
  filter: /^\d+\.\d+$/
requests:
  component-type: pypi
  current-version: 2.20.0
  files: [requirements.txt]
  filter: /^\d+\.\d+\.\d+$/

